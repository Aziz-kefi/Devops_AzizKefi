version: '3.7'
services:

  backend:
    image: adameister98/devops_project:back-latest
    restart: always
    ports:
      - target: 8082
        published: 8082
        protocol: tcp
        mode: host
    networks:
      - devops

networks:
  devops:
    external: true
